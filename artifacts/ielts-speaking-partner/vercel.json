{
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "../../node_modules/.bin/vite build --config vite.config.ts",
  "outputDirectory": "dist/public",
  "build": {
    "env": {
      "PORT": "3000",
      "BASE_PATH": "/"
    }
  },
  "functions": {
    "api/transcribe.ts": {
      "maxDuration": 60,
      "memory": 512
    },
    "api/gemini/chat.ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
